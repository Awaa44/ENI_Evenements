{% extends 'base.html.twig' %}

{% block title %}Listes des utilisateurs{% endblock %}

{% block body %}

    <h1>Utilisateurs</h1>

    <table>
        <thead>
        <tr>
            <th>Nom</th>
            <th>Prénom</th>
            <th>Pseudo</th>
            <th>Téléphone</th>
            <th>Email</th>
            <th>Sites</th>
            <th>Actif</th>
            <th>Rôles</th>
            <th>Actions</th>
        </tr>
        </thead>
        <tbody>
        {% for participant in participants %}
            <tr>
                <td>{{ participant.nom }}</td>
                <td>{{ participant.prenom }}</td>
                <td>{{ participant.pseudo }}</td>
                <td>{{ participant.telephone }}</td>
                <td>{{ participant.email }}</td>
                <td>{{ participant.sites.nomSite }}</td>
                <td>{{ participant.actif ? 'Oui' : 'Non' }}</td>
                <td>{{ participant.roles|join(', ') }}</td>
                <td>
                    <button class="btn btn-warning btn-sm">Rendre inactif</button>
                    <button class="btn btn-danger btn-sm">Supprimer</button>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_home_index') }}" class="btn btn-secondary">Retour</a>

{% endblock %}
